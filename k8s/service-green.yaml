apiVersion: v1
kind: Service
metadata:
  # OrbStack: http://green.hello-k8s.svc.cluster.local:8080
  name: green
spec:
  type: NodePort
  selector:
    app: hello-k8s
    variant: green
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30081
