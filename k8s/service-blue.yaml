apiVersion: v1
kind: Service
metadata:
  # OrbStack: http://web-blue.demo.svc.cluster.local:8080
  name: web-blue
spec:
  type: NodePort
  selector:
    app: web
    variant: blue
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30080
