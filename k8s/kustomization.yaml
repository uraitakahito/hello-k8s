apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hello-k8s
resources:
  # namespace.yaml must be first — other resources depend on this Namespace existing
  - namespace.yaml
  - deployment-blue.yaml
  - deployment-green.yaml
  - service-blue.yaml
  - service-green.yaml
